// @pre (token_from == 1 and token_to == 2)
func g(token_from: felt, token_to: felt) -> () {
    // Calculate swap amount.
    ret;
}

// Swaps tokens between the given account and the pool.
func f(token_from: felt, token_to: felt) -> () {
    g(token_from, token_to);
    ret;
}